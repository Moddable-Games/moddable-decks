# Mod Detail Page

The page a visitor lands on when they click into a specific mod.
Anatomy: dark cosmic header (with mod metadata) → light catalogue body (rules sections, downloads, components list, related mods).

## Files

| File | What it is |
|---|---|
| `index.html` | "Are you ready to nuke Catan?" — a single mod-detail page, fully rendered. |

It pulls components from `../marketing/` (NavBar, Btn, Footer, ModCard) where shared, and adds mod-detail-specific blocks inline.
